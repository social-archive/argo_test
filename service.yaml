# GitHub Repository에 업로드할 예제 Service
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30090  # 8090 포트로 접근 가능 (새 포트 추가 필요)
  selector:
    app: nginx
