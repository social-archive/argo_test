apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  labels:
    app: springboot-app
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30082  # http://localhost:8082 으로 접근
    protocol: TCP
    name: http
  selector:
    app: springboot-app
