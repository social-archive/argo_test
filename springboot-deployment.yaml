apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-developer
  labels:
    app: springboot-developer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springboot-developer
  template:
    metadata:
      labels:
        app: springboot-developer
    spec:
      containers:
      - name: springboot-developer
        image: ghcr.io/social-archive/springboot-developer:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "default"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 30
